export default function ExamHero() {
  return (
    <section className="exam-home-hero" style={{ margin: '1rem 0 2rem', textAlign: 'left' }}>
      <h1
        style={{
          fontSize: 'var(--type-page-title)',
          fontWeight: 'var(--weight-page-heading)',
          letterSpacing: 'var(--tracking-heading)',
          margin: 0,
          maxWidth: 'var(--reading-measure)',
        }}
      >
        Luyện đề thi THPT môn Lịch sử
      </h1>
    </section>
  );
}
