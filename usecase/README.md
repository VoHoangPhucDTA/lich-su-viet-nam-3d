# Current use-case UML index

`usecase/` contains the original project diagrams. The audit backup is in `docs/thesis/output/legacy-usecase/` and was taken before this update.

`usecase_current_system.puml` is the repository-grounded current-system overview linked to UC-001–UC-013. It deliberately labels RAG evaluation as planned and leaves the original diagrams intact. Detailed evidence, actors, classifications and sequence diagrams are under `docs/thesis/output/08_USE_CASE_CATALOG.md`, `09_USE_CASE_SPECIFICATIONS.md` and `uml/`.
