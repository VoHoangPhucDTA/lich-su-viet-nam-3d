# 21 — Open questions requiring owner confirmation

1. Which approved production/staging database snapshot should be used to verify `historical_events.raw_json` against imported `sourceJson`?
2. Is `data/history-rag/v1` expected to be restored from an external artifact, and which baseline counts/hash are authoritative?
3. Which AI provider/model, Chroma collection and retrieval configuration are approved for the thesis demo?
4. Should TTS asset flow be enabled for the submission, and is Viettel token provisioning available in the demo environment?
5. Which ten screenshots and event IDs are approved for insertion into the Word document?
6. What measured response-time, build-size and RAG-quality thresholds should be reported?
7. What is the final deployment topology and whether Cloudinary/OAuth/mail are in scope for evaluation?

These are unresolved external facts, not assumptions filled by this audit.
