# 17 — Chương 5 fact sheet (Kết quả đạt được)

## Verifiable results

- TypeScript build passes; direct Vite production build passes with 4,166 transformed modules.
- Focused terrain/API/exam tests pass (45 tests); exam-data script passes (3 tests).
- Terrain audit parses 361/361 records, resolves 380/380 GADM refs, and calculates conditional eligibility of 136/361.
- Backend package succeeds; backend test run has 190 passes but is not green because one history-RAG package fixture is missing and Docker tests are skipped.

## Claims requiring evidence before insertion

The ten UI figures are placeholders. No screenshot, user-study result, latency percentile, RAG precision/recall/grounding score, TTS availability rate or production database count was verified. Present these as pending measurement, not achieved outcomes. The thesis must also qualify image availability (31/361 records have an image in the static dataset).
